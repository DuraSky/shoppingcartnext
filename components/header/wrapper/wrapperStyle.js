import styled from "styled-components";

export const StyledWrapper = styled.div`
  width: 50%;
`;

export const TopBar = styled.div`
  display: flex;
  justify-content: space-between;
`;

export default { StyledWrapper, TopBar };
